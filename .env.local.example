# Kopyalayın: cp .env.local.example .env.local
# scripts/start.sh bunu otomatik de oluşturur.

NEXT_PUBLIC_RUNTIME_URL=http://127.0.0.1:8000

# Mock hardware (kamera/seri port yokken):
# IO_CAM_MOCK_HARDWARE=1

# VLM prompt / taş sayısı: frontend değil, runtime .env
# → io-cam-runtime/.env  (IO_CAM_VLM_PROMPT, IO_CAM_VLM_MAX_STONES)
